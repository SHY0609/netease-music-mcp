# netease-music-mcp 项目总结

## 这是什么
一个全功能 MCP Server，让 Claude（桌面版+手机App）控制 **网易云音乐播放 + 美团外卖点餐 + 淘宝搜索 + 长期记忆 + Galatea 社交代理 + 屏幕时间追踪**。搜歌、切歌、看歌词、搜外卖、看菜单、下单、搜商品、跨对话记忆、Galatea 论坛/游戏/社交都能用，还能查你今天刷了多久手机。

## 怎么用
1. 打开播放器页面（备案后: `https://yuuke.online/`）
2. Claude Code 项目目录下已配置 MCP（`claude mcp add --scope local yuuke <URL>`）
   - 当前 URL: `https://shore-spokesman-grad-coleman.trycloudflare.com/api/mcp`
   - 备案后换成 `https://yuuke.online/api/mcp`
3. 对 Claude 说 "放一首xxx" 或 "搜一下附近的奶茶" 或 "帮我发个帖子" 或 "我今天刷了多久手机"

## 工具总览 (v3.7)

| 模块 | 工具数 | 说明 |
|------|--------|------|
| 🎵 网易云音乐 | 9 | 搜索/播放/切歌/暂停/队列/歌单/歌词 |
| 🍔 美团外卖 | 4 | 搜索/菜单/地址/**下单（CDP 浏览器自动化，反风控）** |
| 🛒 淘宝 | 3 | 搜索商品/详情/购买链接（**三层回退，主方案开放平台待注册**） |
| 🧠 长期记忆 | 12 | Ombre-Brain 转发（breath/hold/grow...） |
| 🌿 Galatea | 19 | 论坛/回帖/社交互动/棋盘游戏 |
| 📱 屏幕时间 | 2 | iPhone 快捷指令自动采集 + MCP 查询报告 |
| **合计** | **49** | 全部通过一个 MCP 端点暴露 |

## 部署

| 项目 | 详情 |
|------|------|
| 服务器 | 腾讯云轻量应用服务器 2核4G · 40G SSD |
| 系统 | Ubuntu 22.04 |
| 域名 | **yuuke.online**（工信部 ICP 备案中，完成后启用 HTTPS） |
| 当前访问 | Cloudflare Tunnel（`*.trycloudflare.com`，随服务器重启刷新） |
| 进程管理 | systemd（`netease-music-mcp` / `cloudflare-tunnel` / nginx） |
| 记忆库 | Docker（`ombre-brain` 容器，`127.0.0.1:18001`） |
| 备用 | Vercel HK `netease-music-mcp.vercel.app`（已不维护） |

## 项目文件
- `server.js` — **主文件**（腾讯云运行），全功能 HTTP 服务器：MCP + weapi + 美团 CDP + 淘宝 + 播放器 + OB 转发 + Galatea 代理 + 屏幕时间
- `api/index.js` — Vercel 版（旧版，仅备用）
- `lib/netease.js` — 网易云 weapi 加密（国内 IP 全通）
- `lib/mt-signer-v2.cjs` — 美团 mtgsig v1.2 签名生成器（jsdom + h5guard，已弃用，CDP 替代）
- `lib/cdp-meituan.cjs` — **美团 CDP 浏览器自动化**（主方案）：Chromium + WebSocket CDP 操控真实浏览器，模拟真人操作美团 H5
- `lib/runtime.js` — webpack chunk loader（15KB）
- `lib/h5guard-v2.js` — H5guard v1.2 引擎（346KB）
- `lib/taobao.js` — 淘宝 mtop 直连（风控严重，仅作备选）
- `lib/taobao-open.js` — **淘宝开放平台 API**（主方案，不被风控）
- `lib/taobao-browser.js` — Puppeteer 浏览器备选方案
- `lib/bg.js` — 播放器背景图 base64
- `lib/screentime.js` — **屏幕时间追踪**：toggle 采集 + 报告聚合 + 24h 自动清理

## GitHub
- Repo: `https://github.com/SHY0609/netease-music-mcp`
- 分支: `main`（也推到 `master`）

## ✅ 当前版本功能 (v3.6)

### 🌿 Galatea 社交代理（19个工具，转发）
- **Galatea Garden** MCP 代理：论坛/游戏/社交全部工具通过 server.js 透传
- 启动时自动 MCP 握手、工具列表动态获取、调用透明转发
- 会话过期自动重连、Galatea 不可用时优雅降级
- 工具类别：论坛（list_threads/create_thread/create_reply/delete_thread/delete_reply）、社交（interact/list_notifications/list_activity/get_self）、棋盘游戏（list_games/join_game/start_game/submit_action/get_my_status/send_game_chat/leave_waiting_game/get_game_summary/get_tool_schema）
- 环境变量 `GALATEA_TOKEN` 配置，代码内置默认 token 兜底

### 🧠 长期记忆 MCP 工具（12个，Ombre-Brain 转发）
- breath / hold / grow / trace / dream / anchor / release / pulse / plan / letter_write / letter_read / I
- server.js 启动时自动 MCP 握手、工具列表动态获取、调用透明转发
- 会话过期自动重连、OB 不可用时优雅降级
- **LLM（压缩/打标）**: DeepSeek `deepseek-chat`（国内直连）
- **嵌入（语义检索）**: SiliconFlow `BAAI/bge-m3`（充 10 块够用半年）
- **存储**: Markdown + YAML + SQLite 向量索引，Obsidian 兼容

### 🎵 网易云音乐（9个）| 🍔 美团外卖 CDP（4个）| 🛒 淘宝（3个）
- play/skip/pause/queue/playlists/add_to_playlist/playlist_tracks/current_song/lyrics
- mt_search/mt_menu/mt_addresses/mt_order
- tb_search/tb_detail/tb_link
- **美团 CDP 架构**：Chromium 150 + Xvfb + WebSocket CDP，操控真实浏览器 → 美团看到的就是正常用户
- 自动地址切换（`ensurePingxiang`），`lib/cdp-meituan.cjs` 自管理浏览器生命周期
- weapi 加密全通（VIP 歌随便播），mtgsig v1.2 签名引擎（已弃用，仅兜底）
- **淘宝搜索三层回退**：开放平台 API → Puppeteer 浏览器 → mtop 直连
- 淘宝开放平台：使用 `taobao.tbk.dg.material.optional`，官方 API 不被风控

### 📱 屏幕时间追踪（2个 MCP 工具 + HTTP API）
- **原理**：iPhone 快捷指令自动化 → APP 开关时触发 GET 请求 → 服务器 toggle 记录 → Claude MCP 查询
- `screentime_report` — 今日全量报告：每个 APP 的使用时长和次数，按时间排序
- `screentime_app` — 查询单个 APP 今日使用情况
- **toggle 设计**：iPhone 端不区分 open/close，服务器自动取反上一次状态，一个 APP 一条自动化
- **数据存储**：内存 + JSON 文件持久化，自动清理超过 24 小时的记录
- **iPhone 设置**：快捷指令 → 自动化 → App → 勾选"已打开"+"已关闭" → 获取 URL 内容 → `http://139.199.89.55:8765/api/screentime/toggle/APP名`
- **输出示例**：`📱 今日屏幕时间: 122分钟 / 4个APP | 🔴 rednote: 47分钟/3次 🟡 wechat: 23分钟/8次 🟢 Claude: 50分钟/1次`
- 域名备案后切 `https://yuuke.online/api/screentime/toggle/...`

### 🔒 安全加固（2026-07-04）
- Cookie 文件权限 600（仅 owner 可读）
- Ombre-Brain `.env` 权限 600
- SSH 禁用 root 登录、UFW 防火墙开启（22/80/443/8765）
- `taobao-cookie.txt` 已从 Git 追踪移除（历史中仍可见，Cookie 已失效）
- `.gitignore` 规则：忽略 `*cookie*` / `*.txt` / `*.env` / 部署脚本
- 服务器密码已滚动更新
- 8265/8000 等服务端口仅监听 127.0.0.1，不裸奔公网
- Cloudflare Tunnel 提供 HTTPS，不依赖自签证书

### 🔐 MCP 传输链路
| 组件 | 方式 | 状态 |
|------|------|------|
| Claude ↔ 服务器 | Cloudflare Tunnel (HTTPS/TLS) | ✅ |
| 服务器内 MCP | 127.0.0.1:8765 (HTTP plain) | ✅ local only |
| server.js ↔ OB | 127.0.0.1:18001 (HTTP plain) | ✅ local only |
| 外部直接访问 | 80/443 端口由 Nginx 代理 | ✅ |

## ⚠️ 已知限制
- **ICP 备案**: 域名 yuuke.online 备案审核中（1-2 周），完成后换 HTTPS 直连
- **Cloudflare Tunnel**: 服务器重启后域名刷新，备案下来前需持续使用
- **美团 CDP 定位**: 服务端 session 绑定城市，需通过 CDP UI 切换地址（已自动化 `ensurePingxiang`）
- **美团首调耗时**: 首次调用需 ~14s（切换地址+搜索），后续复用 session 约 8s
- **淘宝搜索**: mtop/Puppeteer/SSR 全被 RGV587 风控，等待开放平台 AppKey 完成注册
- **Cookie 有效期**: 美团/网易 Cookie 需定期导出更新
- **GitHub 被墙**: 服务器 git pull 大概率超时，代码更新用 SCP
- **Chromium 依赖**: Xvfb + Snap Chromium，MCP 服务启动时自动拉起管理

## 🔧 修复历史

### v3.8 — iPhone 屏幕时间追踪（2026-07-14）
- **新增 `lib/screentime.js`**：toggle 采集 + 报告聚合 + 24h 自动清理 + 精简摘要输出
- **新增 2 个 MCP 工具**：`screentime_report`（全文报告）、`screentime_app`（单 APP 查询）
- **新增 HTTP API**：`GET /api/screentime/toggle/:app`（iPhone 快捷指令调用）、`GET /api/screentime/report`
- iPhone 快捷指令自动化：APP 打开/关闭时自动发 GET 请求，服务器 toggle 机制自动判断 open/close
- **省 Token 优化**：MCP 工具返回自然语言摘要（`🔴 rednote: 47分钟/3次`）而非完整 JSON，省约 70% token
- 直连 IP `139.199.89.55:8765`，不走 Cloudflare Tunnel，iPhone 国内访问不超时
- 数据 24h 自动清理，JSON 文件持久化，服务器重启不丢

### v3.7 — CDP 浏览器自动化替代 API 签名（2026-07-14）
- **美团方案重构**：从 mt-signer API 逆向 → CDP 浏览器自动化（Chrome DevTools Protocol）
- 原因：美团 `/openh5/order/v2/preview` 始终返回 code 3"商品已下线"，API 逆向被风控识别
- **CDP 架构**：Xvfb 虚拟显示器 + Chromium 150 + WebSocket CDP → 操控 `h5.waimai.meituan.com`
- 浏览器的 cookie/profile 持久化、user-agent 模拟手机、`ensurePingxiang()` 自动地址切换
- **mt_search/mt_menu/mt_order** 全部走 CDP：搜索词填入 + 点击搜索按钮 + DOM 文本提取
- `lib/cdp-meituan.cjs` 自管理 Chromium 生命周期（systemd 不再管），崩了自动重生
- **Token 优化**：搜索结果解析为结构化数据（`parseShops`），菜单只返回"产品名 ¥价格"
- 首次调用 ~14s（含萍乡地址切换），后续复用 session ~8s
- 定位问题：美团服务端 session 锁城市，需 CDP UI 操作切地址（已自动化），Cookie 注入无效
- **删除**：`mt-signer` API 路径已废弃（代码保留兜底），`mt_order` 现在走 CDP 搜索→进店→+号加购→购物车→结算

### v3.6 — 美团下单完成 + 淘宝搜索实测总结（2026-07-08）
- **美团下单流程完善**：preview → create 两步走，preview 成功后拿 orderToken 提交订单
- create 成功返回 `payUrl`（支付链接），失败返回预览价格+错误原因
- 验证 mt_search/mt_addresses 线上可用，preview API 调通
- **淘宝搜索最终结论**：mtop API + Puppeteer 浏览器 + s.taobao.com SSR 全部被风控
- 三层回退机制保留（开放API → 浏览器 → mtop），等开放平台注册完成即用
- 服务器已装 Chromium + puppeteer-core，浏览器方案代码就绪

### v3.5 — Galatea 代理集成（2026-07-07）
- **Galatea Garden MCP 代理**：server.js 启动时连接 Galatea，自动获取 19 个工具
- 采用 OB 同款转发模式：工具列表合并、调用透明转发、session 过期自动重连
- 三级路由：OB 工具 → Galatea 工具 → 本地工具
- `GALATEA_TOKEN` 环境变量配置，代码内置默认值兜底
- 非阻塞启动，Galatea 不可用时不影响其他功能

### v3.4 — 淘宝开放平台 API 接入（2026-07-05）
- **接入官方开放平台**：`taobao.tbk.dg.material.optional` 物料搜索，不被风控
- 签名算法：MD5(key+secret排序)，app_key/app_secret/adzone_id 从环境变量加载
- 新增 `lib/taobao-open.js`，server.js 三层回退（开放API → 浏览器 → mtop）
- 搜索结果带优惠券金额、佣金比例、原始价格等额外字段
- 已知限制：需注册淘宝开放平台+淘宝联盟，获取 appkey/secret/adzone_id

### v3.3 — 淘宝模块完善（2026-07-04）
- **tbSearch 搜索结果解析**：mtop h5search 返回的 URL-encoded JSON 现在正确解析为结构化商品数据
- 多字段名兼容（itemId/item_id/nid, price/view_price, pictUrl/pic_url 等）
- 深度搜索兜底：关键词匹配失败时递归查找首层商品数组
- 客户端价格过滤（minPrice/maxPrice）
- **tbDetail 增强**：补全 subTitle/originalPrice/stock/shopId/shopScore/deliveryInfo/coupons 字段
- **新增 tb_link 工具**：从 itemId 生成 H5 + PC 购买链接
- tb_search/tb_detail handler 添加重试逻辑（对标美团模式）
- 结果整形：标题截断 60 字、店铺名截断 20 字、最多 15 条

### v3.2 — 安全加固 + 隧道临时代理（2026-07-04）
- 全面安全审计：修复 Cookie/API Key 权限、禁用 root SSH、开启 UFW
- GitHub 泄露清理：taobao-cookie.txt 移除 + .gitignore 更新
- Cloudflare Tunnel 搭建：临时 HTTPS 通道（解决备案期间无 HTTPS 问题）
- 服务器密码滚动、MCP 服务名改为 Yuuke
- SiliconFlow 充值 + 向量化 403 修复

### v3.1 — Ombre-Brain 记忆库集成（2026-07-03）
- Docker 部署 + 12 记忆工具完整可用
- LLM 方案：Gemini（被墙）→ Workers（被墙）→ Vercel（被墙）→ 最终 DeepSeek + SiliconFlow
- SSE 解析适配、会话过期自动重连

### v3.0 — 腾讯云部署 + HTTPS（2026-07-03）
### v2.3 — 美团下单完善（2026-07-02）
### v2.x — mtgsig 逆向（2026-06-22）

## 🔧 服务器维护
```bash
# SSH
ssh ubuntu@139.199.89.55

# MCP 日志
sudo journalctl -u netease-music-mcp -f

# CDP Chromium 状态
curl -s http://127.0.0.1:9222/json/version | grep Browser
curl -s http://127.0.0.1:9222/json | grep title

# Xvfb 虚拟显示器
systemctl status xvfb
ps aux | grep Xvfb

# Cloudflare 隧道日志 / 刷新域名
sudo journalctl -u cloudflare-tunnel -n 30 | grep trycloudflare

# 更新代码（SCP 传文件 — server.js + cdp-meituan.cjs）
scp server.js lib/cdp-meituan.cjs ubuntu@139.199.89.55:/home/ubuntu/netease-music-mcp/
sudo systemctl restart netease-music-mcp

# 更新 Cookie
sudo nano /etc/netease-music-mcp/env
sudo systemctl restart netease-music-mcp

# Ombre-Brain
docker ps | grep ombre
cd ~/ombre-brain && docker compose -f docker-compose.user.yml restart

# 屏幕时间数据查询 / 清理
curl -s http://139.199.89.55:8765/api/screentime/report
cat /tmp/screentime-data.json   # 原始 JSON
sudo rm -f /tmp/screentime-data.json  # 清空数据
sudo systemctl restart netease-music-mcp

# 重置 CDP 浏览器（清空 profile）
sudo systemctl stop netease-music-mcp
pkill -f chromium.*9222
rm -rf /home/ubuntu/chrome-cdp-profile
sudo systemctl start netease-music-mcp
```

## 🔑 未来方向
1. **ICP 备案完成** → `https://yuuke.online/api/mcp` 直连，去掉 Cloudflare Tunnel
2. **CDP 下单支付验证**: 完整跑通加购→结算→提交→支付全链路
3. **多商品下单**: 购物车累积多件商品后统一结算
4. **淘宝 CDP**: 用同款 CDP 方案解决淘宝 RGV587 风控
5. **Cookie 自动刷新**: 定时从浏览器同步，避免手动更新
6. **Chromium 替换**: 改用 Playwright 专用版，避免 Snap 沙箱问题
