# netease-music-mcp 项目总结

## 这是什么
一个全功能 MCP Server，让 Claude（桌面版+手机App）控制 **网易云音乐播放 + 美团外卖点餐 + 淘宝搜索 + 抖音搜索/热搜/视频/主页/评论 + 长期记忆 + Galatea 社交代理 + 屏幕时间追踪**。

## 怎么用
1. MCP 地址: `https://yuuke.online/api/mcp`（永久不变）
2. VNC 远程桌面: `https://yuuke.online/vnc/vnc_lite.html?path=vnc/websockify`（手机直接打开）
3. 对 Claude 说 "搜一下周杰伦的歌" 或 "接受一起听邀请" 或 "给我的网易云好友发私信"

## 工具总览 (v4.6)

| 模块 | 工具数 | 说明 |
|------|--------|------|
| 🎵 网易云音乐 | 14 | 搜索/歌单管理/歌词 + 一起听(接受/切歌/加歌/心跳) + 私信收发 |
| 🍔 美团外卖 | 4 | 搜索/地址/菜单/下单（API 优先 + VNC+CDP hybrid 兜底） |
| 🛒 淘宝 | 3 | 搜索商品/详情/购买链接（三层回退） |
| 🎵 抖音 | 5 | search/trending/video/user/comment（CDP+VNC hybrid） |
| 🧠 长期记忆 | 12 | Ombre-Brain 转发（breath/hold/grow/trace/dream/anchor/release/pulse/plan/letter_write/letter_read/I） |
| 🌿 Galatea | ~23 | 论坛/回帖/私信/游戏/社交互动（动态注册，数量浮动） |
| 📱 屏幕时间 | 2 | iPhone 快捷指令自动采集 + MCP 查询报告 |
| **合计** | **~63** | 内置 28 个 + Galatea ~23 + 记忆库 12 |

### 网易云工具详情 (14个)

| 工具 | 功能 | 后端 |
|------|------|------|
| `ncm_search` | 搜索歌曲 | Docker :3939 |
| `ncm_song_detail` | 歌曲详情 + 完整歌词 | Docker |
| `playlists` | 获取我的歌单 | Docker |
| `playlist_tracks` | 查看歌单内容 | Docker |
| `add_to_playlist` | 加歌到歌单 | Docker |
| `ncm_accept_invite` | 接受一起听邀请 | eapi 直连 |
| `ncm_leave_room` | 离开一起听房间 | eapi 直连 |
| `ncm_room_status` | 查看房间状态 | eapi 直连 |
| `ncm_switch_song` | 房间内切歌 ✅ | eapi 直连 |
| `ncm_add_song` | 加歌到房间队列 ✅ | eapi 直连 |
| `ncm_heartbeat` | 手动启停心跳 | eapi 直连 |
| `ncm_send_message` | 发私信 | Docker |
| `ncm_read_messages` | 读私信(含解析邀请) | Docker |
| `ncm_message_list` | 私信联系人列表 | Docker |

> ✅ v4.6：加歌/切歌通过 `NCM_HOST_COOKIE`（房主 VIP cookie）代理，已全闭环。

## 部署

| 项目 | 详情 |
|------|------|
| 服务器 | 腾讯云轻量应用服务器 2核4G · 40G SSD |
| 系统 | Ubuntu 22.04 |
| 域名 | **yuuke.online**（✅ ICP 备案已通过） |
| 访问 | nginx + Let's Encrypt HTTPS 直连 |
| 远程桌面 | x11vnc + noVNC → `https://yuuke.online/vnc/vnc_lite.html` |
| 进程管理 | systemd（`netease-music-mcp` / nginx / Xvfb / x11vnc / websockify） |
| NCM Docker | `binaryify/netease_cloud_music_api:latest` (port 3939)，读操作代理 |
| 记忆库 | Docker（`ombre-brain` 容器，`127.0.0.1:18001`） |

### 网易云架构 (v4.6)

```
读操作 (搜索/歌单/歌词) → Docker :3939 → 绕过 CDN
写操作 (一起听)         → eapi 直连 → interface.music.163.com
私信                   → Docker :3939 → 绕过 CDN
```

原 weapi (music.163.com/weapi/*) 被火山引擎 CDN 拦截 (200 空 body)，改走 Docker 代理。

## 项目文件
- `server.js` — **主文件**（腾讯云运行），~980行，全功能 HTTP 服务器
- `lib/netease.js` — 网易云 **weapi + eapi 双加密**（AES-128-CBC + AES-128-ECB）+ Docker 代理
- `lib/cdp-meituan.cjs` — **美团 CDP 浏览器自动化**：Chromium + WebSocket CDP，含 Crash Watchdog + DOM 等待
- `lib/cdp-douyin.cjs` — **抖音 CDP+VNC hybrid**：CDP 鼠标点击 + 键盘输入，Chrome profile 持久登录，含 Crash Watchdog + 轻量序列化
- `lib/domain-skills/` — **平台交互模式 JSON 配置**（对标 Browser-Use）：CDP 基础设施 / 抖音搜索 / 评论 / 用户 / 美团全流程
- `lib/taobao.js` — 淘宝 mtop 直连（备选）
- `lib/taobao-open.js` — 淘宝开放平台 API（主方案）
- `lib/taobao-browser.js` — Puppeteer 浏览器备选
- `lib/mt-signer-v2.cjs` — 美团 mtgsig 签名（已弃用，CDP 替代）
- `lib/screentime.js` — 屏幕时间追踪：toggle 采集 + 报告聚合

## GitHub
- Repo: `https://github.com/SHY0609/netease-music-mcp`
- 分支: `main`

## ✅ 当前版本功能 (v4.6)

### 🎵 抖音（5个核心工具，CDP+VNC hybrid）
- `dy_search` — 搜索抖音视频：SPA 内搜索，返回作者、标题、点赞数
- `dy_trending` — 抖音热搜榜：当前最火话题和视频排行（50条）
- `dy_video` — 视频详情：首页点击视频卡 → overlay 弹窗 → 提取内容
- `dy_user` — 用户主页：CDP 鼠标点击"我的"，返回昵称/粉丝/获赞/作品；`userId='following'` 看关注列表
- `dy_comment` — 视频评论：搜索关键词 → CDP 点击打开视频 → CDP 键盘逐字输入 → 发送
- **搜索技术**：`input` → `change` → `keydown(Enter, keyCode:13)` 事件序列触发 React SPA 搜索
- **登录方案**：x11vnc + noVNC 远程桌面 → 人类扫码一次 → Chrome profile 永久保存
- **评论技术（v4.1 修复）**：评论区是 **Draft.js 富文本编辑器**（`[contenteditable=true]`），发送按钮是**箭头图标**（`commentInput-right-ct` 末位 SPAN）。必须 CDP `keyDown+char+keyUp` 逐字输入才能触发 Draft.js onChange 显示发送按钮。`execCommand('insertText')` 和合成事件均无效
- **VNC+CDP hybrid**：CDP 自己 `Page.navigate` 打开的 tab **不渲染评论区组件**，必须扫描复用 VNC 用户已打开的 tab（URL 含 `modal_id` 或 `/note/`），通过 `httpGet(:9222/json)` 找到后 WS 直连
- **抖音内容类型**：`/video/{id}` 可能是图文 Note 会自动重定向到 `/note/{id}`。需同时支持两种 URL

### 🌿 Galatea 社交代理（20个工具，转发）
- Galatea Garden MCP 代理：论坛/游戏/社交全部工具透传
- 启动时自动 MCP 握手、会话过期自动重连

### 🧠 长期记忆 MCP 工具（12个，Ombre-Brain 转发）
- breath / hold / grow / trace / dream / anchor / release / pulse / plan / letter_write / letter_read / I
- LLM（压缩/打标）: DeepSeek `deepseek-chat`（国内直连）
- 嵌入（语义检索）: SiliconFlow `BAAI/bge-m3`

### 🍔 美团外卖（4个，API 优先 + VNC+CDP hybrid）
- `mt_search` — 搜索店铺（API 优先，CDP 搜索常被风控）
- `mt_addresses` — 获取配送地址（API，可用）
- `mt_menu` — 查看店铺菜单（CDP+VNC tab）
- `mt_order` — 下单（API 优先，返回 payUrl 付款链接）
- **VNC+CDP hybrid**（2026-07-20 验证）：和抖音评论同方案——VNC 真人打开美团 → CDP 连到同一 tab → 不受风控。已验证可读菜单、点击加购

### 🎵 网易云音乐（14个）| 🛒 淘宝（3个）
- 搜歌/加歌/切歌/歌单/私信/一起听全闭环，详见历史版本记录

### 📱 屏幕时间追踪（2个 MCP 工具 + HTTP API）
- iPhone 快捷指令自动化 → APP 开关时触发 GET 请求 → 服务器 toggle 记录 → Claude MCP 查询
- `screentime_report` / `screentime_app`

## ⚠️ 已知限制
- **网易云切歌加歌**：✅ v4.6 已解决 — 通过 NCM_HOST_COOKIE（房主 VIP cookie）代理，加歌/切歌全闭环
- **网易云 weapi**：火山引擎 CDN 静默拦截（200 空 body），读操作走 Docker 绕过
- **美团搜索**: API 和 CDP 直连均被风控，需 VNC 真人打开后 CDP 复用 tab
- **淘宝搜索**: 开放平台 API 已就绪，待注册 AppKey
- **dy_comment 依赖 VNC tab**: CDP 自己开的 tab 评论区不渲染，需 VNC 用户先打开页面
- **Chromium**: Snap 版不能用 systemd 管理，只能 Node.js spawn()
- **GitHub 被墙**: 服务器 git pull 大概率超时，代码更新用 SCP

## 🔑 未来方向
1. **通用浏览器 MCP 工具**: 封装 VNC+CDP 为标准化工具（navigate/click/type/screenshot）
3. **美团下单全链路**: 搜索→加购→结算→支付，基于 VNC+CDP hybrid
4. **淘宝 CDP**: 用同款 VNC+CDP 方案解决 RGV587 风控
5. **Chromium 替换**: 改用非 Snap 版，支持 systemd 管理

## 🧠 借鉴 Browser-Use 的改进 (v4.2)

分析 [Browser-Use](https://github.com/browser-use/browser-use) 项目后，借鉴了以下设计：

| 改进 | 说明 | 效果 |
|------|------|------|
| 🔥 **Crash Watchdog** | WebSocket `close`/`error` 监听 → `crashed` 标记 → `ensureDouyin()`/`ensurePingxiang()` 自动重建 session | Chromium 崩溃后不挂，自动恢复 |
| ⏱️ **waitForDomStable()** | Runtime.evaluate 轮询替代固定 sleep()，超时 fallback | dy_search 从 15s → 5-8s |
| 📋 **搜索结果编号化** | `dySearch` 视频加 `index` 字段 + 轻量 DOM 序列化 fallback | 解析失败时 Claude 仍能看到结构化卡片 |
| 📁 **Domain Skills** | `lib/domain-skills/` 形式化各平台交互模式（CDP 基础设施/抖音搜索/评论/用户/美团全流程） | 改版时只改 JSON 不翻源码 |

详细分析见 [Browser-Use vs netease-music-mcp 对比](./BROWSER-USE-ANALYSIS.md)（待补充）。

### v4.4 — 一起听 + 私信 + Docker 架构（2026-07-21）
- **Web 播放器移除**：删除 play/skip/pause/queue/current_song/lyrics，播放器 HTML 和路由全部清理
- **一起听接入**：ncm_accept_invite / ncm_leave_room / ncm_room_status / ncm_switch_song / ncm_add_song / ncm_heartbeat — 接受邀请、加入房间、切歌加歌、心跳保活
- **私信接入**：ncm_send_message / ncm_read_messages / ncm_message_list — 收发私信、解析邀请
- **架构变更**：weapi (music.163.com) 被火山引擎 CDN 拦截 → 读操作走 Docker (binaryify/netease_cloud_music_api:3939)，写操作走 eapi 直连 (interface.music.163.com)
- **代码清理**：删除 server.js 中重复的网易云函数（~80行），统一走 lib/netease.js；删除 Web 播放器 HTML（~35行）
- **lib/netease.js**：修复 eapi 加密（MD5 + ECB + uppercase hex），新增 10 个 API 函数，Docker 代理层
- **工具统计**：网易云 9→14 (+5)，总内置 23→28 (+5)
- **已知限制**：客人账号无法切歌加歌（需 VIP），weapi 被 CDN 拦（走 Docker 绕过）

### v4.6 — 加歌突破：读 Docker 源码逆推正确格式（2026-07-21）
- **➕ 加歌彻底修复**：`ncm_add_song` 实时生效（已验证）
  - 关键突破：`docker exec ncmapi cat /app/module/listentogether_sync_list_command.js` 读到源码
  - `playlistParam` 正确格式：`{commandType:"ADD", version:[{userId,version}], anchorSongId, anchorPosition:0, randomList:[], displayList:[全量歌单]}`
  - 流程：获取当前列表 → 追加新歌 → **递增版本号** → 全量提交
  - 不是增量加歌，是全量替换列表
- **🔀 切歌格式修正**：对齐 `listentogether_play_command.js` 源码
  - `commandInfo`：`{commandType:"GOTO", targetSongId, formerSongId, progress, playStatus, clientSeq}`
  - 字段名是 `commandType` 不是 `type`，需要 `formerSongId` + `clientSeq` 递增
  - **不需要 `header` 字段**（之前一直错误地加了）
- **✅ 全流程打通**：搜歌 → 加歌 → 切歌，全自动闭环
  - 验证通过：晴天(186016)、盛夏的果实(277382)、表白(307081)

### v4.5 — Docker Cookie + 房主代理 + 手机端优化（2026-07-21）
- **🍪 房主 Cookie 代理**：新增 `NCM_HOST_COOKIE` 环境变量
  - 加歌/切歌走房主 VIP cookie，搜歌/私信仍走 bot cookie
- **🐳 Docker Cookie 修复**：`dockerGet` 从 `?cookie=` 查询参数改为 `Cookie` 请求头
- **📱 手机端 token 优化**：响应瘦身 45-96%
  - `ncm_read_messages`：砍掉完整 user profile → 只保留 nickname/msg/type/time（-87%）
  - `ncm_room_status`：砍掉原始 JSON → 只返回 songId/status/playlistTop5（-96%）
  - `ncm_search`：砍掉 album/coverUrl/durationMs → 只保留 id/name/artist（-45%）

### v4.3 — HTTPS 直连 + 美团 VNC+CDP + 工具精简（2026-07-20）
- **HTTPS 永久域名**: 弃用 Cloudflare Quick Tunnel，nginx + Let's Encrypt → `https://yuuke.online` 永久不变
- **VNC 手机端**: nginx 代理 `/vnc/` → `https://yuuke.online/vnc/vnc_lite.html`，手机直接打开
- **dy 工具精简**: 10→5，删除 dy_cookie/login/switch/notifications/messages
- **mt_addresses 修复**: 补上缺失的 case handler（之前注册了但从未生效，始终返回 "Unknown tool"）
- **美团 API 优先**: mt_search/mt_order 改为 API 优先（CDP 被风控时 fallback），mt_order 下单返回 payUrl
- **美团 VNC+CDP 验证**: CDP 连接 VNC 真人打开的 tab → 成功读取菜单、点击加购（和抖音评论同方案）

## 🔧 修复历史

### v4.2 — Browser-Use 借鉴 + Crash Watchdog + DOM 等待（2026-07-19）
- Crash Watchdog、waitForDomStable()、dySearch 编号化、Domain Skills 形式化
- cloudflared 升级到 2026.7.2，尝试命名隧道失败

### v4.1 — dy_comment 彻底修复 + 回复功能（2026-07-17～19）
- **Bug 根因**：旧 `dy_comment` 完全忽略 videoId；CDP 键盘对 Draft.js 无效；发送按钮是箭头图标；评论区依赖 VNC 渲染
- **顶层评论**：VNC+CDP hybrid → `httpGet(:9222/json)` 扫 VNC tab（`/note/`、`/article/`、`modal_id`）→ CDP 点 `contenteditable` → `keyDown+char+keyUp` 逐字输入 → 发送
- **回复子评论**：`replyTo` 参数 → `scrollIntoView` + `focus` 确保渲染 → 搜含关键词的"回复"SPAN（`x<240` 区分主/子评论）→ JS `parentElement.click()` 触发"回复中"→ 验证状态保住 → 编辑器逐字输入 → 发送（优先 `wchsYBpK` class，兜底 36x36 SPAN）
- **内容类型**：Note（`/note/`）、Article（`/article/`）、Video（重定向到 Note）全部支持
- **发送按钮**：通用 class `wchsYBpK.jfGCpJo0`（36x36 箭头 SVG），JS click 触发。回复模式下可能消失，兜底搜任意 36x36 SPAN
- **踩坑记录**：
  - `comment-item.parentElement` 只有"0分享回复"，需往上 4-5 级取完整评论文本
  - CDP 鼠标点 SPAN 无效，必须 JS `parentElement.click()`
  - `y>300` 过滤排除滚出屏幕的评论（负值）；`y>200` 排除顶部评论区
  - 回复状态下 CDP 点编辑器容易误触取消"回复中"→ 需 `x<300` 避开按钮区域 + 点击后验证
  - `@用户名` 中 Unicode 全角字符（U+FF1F `？`）可能导致文本匹配失败，用评论旁唯一文字（如"互相关注""抢首评"）
  - `execCommand('insertText')` 和合成事件均无法触发 Draft.js onChange
  - 抖音审核可能拦截含"测试"字样的评论

### v4.0 — 抖音 CDP 完整实现（2026-07-17）
- **抖音登录方案**：x11vnc + noVNC 远程桌面，人类扫码一次，Chrome profile 永久保存
- **dy_search 修复**：`input`→`change`→`keydown(Enter,keyCode:13)` 事件序列触发 React SPA 搜索
- **dy_user 实现**：CDP 鼠标点击"我的"按钮，React SPA 导航成功
- **dy_comment 实现**：CDP Input.dispatchKeyEvent 逐字输入 → 找发送按钮
- **dy_video 实现**：首页点击视频卡 → overlay 弹窗模式
- **关键发现**：桌面版抖音是 React SPA + PACE-ISLAND + Semi Design 微前端，URL 跳转全废
- **安全加固**：x11vnc 仅监听 localhost，VNC 密码文件权限 600，SSH 隧道加密
- ⚠️ dy_notifications/messages 因 PACE-ISLAND CDP 拦截未完成，dy_login 已废弃改用 dy_cookie，但均未从注册表中移除

### v3.9 — 抖音 CDP 模块初版（2026-07-16）
- 新增 `lib/cdp-douyin.cjs`（~500 行），对标美团 CDP 架构
- PUT /json/new（新版 Chromium）+ Stealth 反检测 + 持久 session
- 已知限制：视频页/用户页 URL 不工作（v4.0 修复为 SPA 点击）

### v3.8 — iPhone 屏幕时间追踪（2026-07-14）
### v3.7 — CDP 浏览器自动化替代 API 签名（2026-07-14）
### v3.6 — 美团下单完成 + 淘宝搜索（2026-07-08）
### v3.5 — Galatea 代理集成（2026-07-07）
### v3.4 — 淘宝开放平台 API 接入（2026-07-05）
### v3.3 — 淘宝模块完善（2026-07-04）
### v3.2 — 安全加固 + 隧道（2026-07-04）
### v3.1 — Ombre-Brain 记忆库集成（2026-07-03）
### v3.0 — 腾讯云部署 + HTTPS（2026-07-03）

## 🔧 服务器维护
```bash
# SSH
ssh ubuntu@139.199.89.55

# MCP 日志
sudo journalctl -u netease-music-mcp -f

# CDP Chromium 状态
curl -s http://127.0.0.1:9222/json/version | grep Browser

# NCM Docker 状态
sudo docker ps | grep ncmapi

# Xvfb 虚拟显示器
systemctl status xvfb

# 更新代码（SCP）
scp server.js lib/netease.js lib/cdp-meituan.cjs lib/cdp-douyin.cjs ubuntu@139.199.89.55:/home/ubuntu/netease-music-mcp/
sudo systemctl restart netease-music-mcp

# 更新 cloudflared（本地下载后SCP上传，GitHub被墙）
curl -sL -o cloudflared-linux https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64
scp cloudflared-linux ubuntu@139.199.89.55:/tmp/cloudflared-new
ssh ubuntu@139.199.89.55 "sudo mv /tmp/cloudflared-new /usr/local/bin/cloudflared && sudo chmod +x /usr/local/bin/cloudflared && sudo systemctl restart cloudflare-tunnel"

# 本地更新 MCP Tunnel 地址
node update-tunnel-url.cjs

# 重置 CDP 浏览器
sudo systemctl stop netease-music-mcp
pkill -f chromium.*9222
rm -rf /home/ubuntu/chrome-cdp-profile
sudo systemctl start netease-music-mcp

# 屏幕时间
curl -s http://139.199.89.55:8765/api/screentime/report

# VNC 远程桌面
# 手机/电脑浏览器直接打开:
# https://yuuke.online/vnc/vnc_lite.html?path=vnc/websockify
# 或本地 SSH 隧道:
sudo systemctl start x11vnc
# 本地: ssh -L 6080:127.0.0.1:6080 ubuntu@139.199.89.55
# 浏览器: http://localhost:6080/vnc_lite.html → Connect
```

## 🔑 未来方向
1. **通用浏览器 MCP 工具**: 封装 VNC+CDP 为标准化工具（navigate/click/type/screenshot），让 Claude 自由操控美团和抖音
2. **美团下单全链路**: 搜索→加购→结算→支付，基于 VNC+CDP hybrid
3. **淘宝 CDP**: 用同款 VNC+CDP 方案解决 RGV587 风控
4. **Chromium 替换**: 改用非 Snap 版，支持 systemd 管理
