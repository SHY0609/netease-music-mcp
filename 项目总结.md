# netease-music-mcp 项目总结

## 这是什么
一个 MCP Server，让你用 Claude（网页版+手机App）控制网易云音乐播放 + **美团外卖点餐**。搜歌、切歌、看歌词、搜外卖、看菜单、算价格、下单都能用。

## 怎么用
1. 手机浏览器打开 `https://netease-music-mcp.vercel.app/`（播放器页面）
2. Claude 网页版 Settings → MCP Servers → 添加：
   - Name: `netease-music`
   - URL: `https://netease-music-mcp.vercel.app/api/mcp`
3. 对 Claude 说 "放一首xxx" 或 "帮我搜附近的汉堡"

## 项目文件
- `api/index.js` — Vercel 服务端函数（MCP + 播放器 HTML + 网易云接口 + 美团接口）
- `api/index.js.backup` — 早期备份（v1.3.x 样式）
- `api/index.js.backup2` — 后期备份（v1.5.x 毛玻璃样式 + localStorage）
- `lib/netease.js` — 网易云 weapi 加密
- `lib/bg.js` — 背景图 base64（6.jpg）
- `lib/mt-signer-v2.cjs` — 美团 mtgsig v1.2 签名生成器（jsdom + h5guard）
- `lib/runtime.js` — webpack chunk loader（浏览器抓取，15KB）
- `lib/h5guard-v2.js` — H5guard v1.2 引擎（CDN 下载，346KB）
- `lib/h5guard.js` — 旧版 h5guard（241KB，辅助包）
- `server.js` — Koyeb 版本（weapi全功能，未部署）
- `vercel.json` — Vercel 配置（香港 hkg1）
- `D:\Claude Code\cookie.txt` — 完整 Cookie（2600字符）
- `D:\Claude Code\meituan-cookie.txt` — 美团 Cookie（1807字符）

## GitHub
- Repo: `https://github.com/SHY0609/netease-music-mcp`
- 分支: `main`（也推到 `master`）
- Vercel 连着 GitHub，推送自动部署

## Vercel
- 项目: hailey9/netease-music-mcp
- 区域: **香港 hkg1**（vercel.json functions.regions）
- URL: `https://netease-music-mcp.vercel.app`
- 环境变量: `MEITUAN_COOKIE`（美团Cookie）、`NETEASE_COOKIE`（网易Cookie）
- Debug 端点: `/api/debug?mt=关键词` 自定义搜索测试

## ✅ 当前版本功能 (v2.x)

### 🎵 网易云音乐 MCP 工具
- play — 搜索并播放（4 级 URL 回退：320k→128k→v1→outer）
- skip — 切下一首
- pause — 暂停/恢复
- current_song — 当前歌曲+播放进度+歌词
- lyrics — 完整歌词
- playlists — 获取歌单列表
- playlist_tracks — 获取歌单内歌曲（分页支持）
- add_to_playlist — 添加到歌单
- queue — 队列状态

### 🍔 美团外卖 MCP 工具
- mt_search — 搜索店铺（code:0，20家店+菜品+SKU ID+价格）
- mt_menu — **获取店铺完整菜单**（自动获取tag，所有店铺通用）
- mt_addresses — 获取收货地址（code:0，真实地址）
- mt_order — 预览/确认下单（价格计算成功，支持备注）

### 🎵 播放器前端
- **毛玻璃 UI** — `backdrop-filter: blur`，半透明卡片
- **背景图** — 6.jpg 全屏 + 暗色渐变遮罩
- **专辑封面** — `extractCover()` 4 种方式构造
- **localStorage 持久化** — 刷新不丢歌
- **清空按钮** — 队列标题旁，5 秒防回弹
- **local mcpSwitch** — 浏览器本地 `nm_lastMcp` 时间戳判断 MCP 切歌指令
- **版本化 URL** — `/?v=1.5.1` 强制绕过手机缓存

### 🔐 mtgsig v1.2 签名引擎
- **架构**: jsdom 模拟浏览器 → runtime.js (webpack) → H5guard.js (v1.2引擎)
- **签名速度**: ~900ms 首次，后续 ~15ms
- **依赖**: jsdom@22, jsdom-browser.screen, cookie
- **关键文件**: `lib/mt-signer-v2.cjs`, `lib/runtime.js`, `lib/h5guard-v2.js`
- **原理**: runtime.js 创建 webpack 模块系统 → H5guard.js 注册 → window.H5guard.sign() 生成 v1.2 格式签名（10字段：a1~a10, x0, d1）
- **验证**: code:0，搜索结果真实数据，下单价格计算通过

### 🍔 美团 API 接口清单

| 接口 | 端点 | 状态 | 说明 |
|------|------|------|------|
| 搜索 | `/openh5/search/globalpage` | ✅ code:0 | 返回20家店+菜品+SKU+价格 |
| 地址 | `/openh5/address/list` | ✅ code:0 | 返回用户保存的收货地址 |
| 初始化 | `/openapi/v1/poi/food` | ✅ code:0 | **返回所有 tag 列表，不需要 tag 参数** |
| 菜单 | `/openh5/v2/poi/menuproducts` | ✅ | 用 poi/food 获取的 tag，调此接口拿完整菜单 |
| 下单预览 | `/openh5/order/v2/preview` | ✅ code:0 | 价格计算成功，支持多规格 |
| 下单提交 | `/openh5/order/v2/submit` | ⚠️ 待验证 | 参数格式已有，confirm=true触发 |

### 🔑 关键突破

1. **mtgsig v1.2 签名引擎**: jsdom + runtime.js + H5guard.js 在 Node.js 中生成与浏览器一致的签名
2. **搜索结果自带 SKU ID**: `product_list[].product_sku_id`，无需菜单 API 即可获取
3. **`/openapi/v1/poi/food` 是万能钥匙**: 不需要 tag 参数，返回店铺的完整 tag 列表（`food_spu_tags`），再用第一个 tag 调 menuproducts 拿完整菜单
4. **菜单自动获取方案**: poi/food → 获取 tags → menuproducts（带 tag）→ 完整菜单+SKU+attrs → 下单
5. **混合下单**: 菜单 SKU + 菜单 attrs = code:0 价格预览

## ⚠️ 已知限制
- **Vercel 冷启动**: jsdom 初始化 ~2s，首次请求可能有延迟。预初始化已缓解此问题
- **菜单 tag 遍历**: 如果第一个 tag 无产品，会遍历后续 tag（可能增加延迟）
- **VIP 歌曲**: Vercel 旧 API（非 weapi）部分 VIP 歌可能无播放链接
- **下单提交**: 未真实验证（避免误下单扣款）
- **手机缓存**: Safari 无视 Cache-Control，建议无痕模式
- **多商品下单**: 当前仅支持单商品，需扩展 foodlist

## 🔧 关键修复历史

### v2.x — 美团外卖集成 + mtgsig v1.2 逆向（2026-06-22）
- mtgsig v1.2 签名引擎（jsdom + runtime + H5guard）：9 小时攻克
- 美团搜索/地址/菜单/下单全套 API
- **万能菜单方案**: `/openapi/v1/poi/food` 自动获取 tag → `menuproducts` 拿完整菜单
- 搜索、菜单、下单预览均 code:0 通过
- 支持备注（remark）
- debug 端点支持 `?mt=关键词` 自定义测试
- 支持所有店铺类型（汉堡、奶茶、炸鸡、烧烤等）

### v1.5.x — UI 重做 + localStorage
- 毛玻璃播放器、6.jpg 背景、琥珀色主题
- localStorage 持久化队列
- 清空按钮 + 5 秒防回弹
- local mcpSwitch 自动切歌

### v1.3.x — 状态重构
- /tmp/ → 纯内存状态
- add_to_playlist trackIds JSON 数组格式

### v1.2.0 — 基础功能
- 全部 MCP 工具可用

## ⚡ 部署注意事项
1. **推送前必须** `node --check api/index.js` 验证语法
2. **MT_COOKIE 声明顺序**: 必须在 `const MT_COOKIE = ...` 之后才能使用
3. 推 `main` 和 `master` 分支（Vercel 从 `master` 拉）
4. Vercel 构建日志会显示 "Restored build cache" — 正常

## 🔑 未来方向
1. **下单提交验证**: 用真实地址测试 mt_order confirm=true
2. **多商品下单**: 支持 foodlist 多个商品同时下单
3. **优惠券集成**: order preview 已返回券信息，需集成到下单流程
4. **搜索结果排序优化**: 按距离/价格排序
5. 迁移到 Koyeb/Fly.io 亚洲服务器，解锁 weapi → VIP 歌曲全通
