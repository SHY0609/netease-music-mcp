# netease-music-mcp 项目总结

## 这是什么
一个 MCP Server，让你用 Claude（网页版+手机App）控制网易云音乐播放。搜歌、切歌、看歌词都能用。

## 怎么用
1. 手机浏览器打开 `https://netease-music-mcp.vercel.app/`（播放器页面）
2. Claude 网页版 Settings → MCP Servers → 添加：
   - Name: `netease-music`
   - URL: `https://netease-music-mcp.vercel.app/api/mcp`
3. 对 Claude 说 "放一首xxx"

## 项目文件
- `D:\Claude Code\netease-music-mcp\` 
- `api/index.js` — Vercel 服务端函数（MCP + 播放器 HTML + 网易云接口）
- `lib/netease.js` — 网易云 weapi 加密（weapi 在新加坡可用，美国不可用）
- `server.js` — Koyeb 版本（weapi全功能，未部署）
- `D:\Claude Code\cookie.txt` — 完整 Cookie（2600字符）
- `vercel.json` — Vercel 配置

## GitHub
- Repo: `https://github.com/SHY0609/netease-music-mcp`
- Vercel 连着 GitHub，推送自动部署

## Vercel
- 项目: hailey9/netease-music-mcp
- URL: `https://netease-music-mcp.vercel.app`
- 环境变量: `NETEASE_COOKIE`（从 cookie.txt 复制）

## 部署方式
- `git push` 到 GitHub → Vercel 自动部署
- CLI `npx vercel --prod --yes` 目前连不上（TLS问题），用 GitHub 推送代替

## ✅ 已跑通的功能
- play — 搜索并播放
- skip — 切歌
- pause — 暂停
- current_song — 当前进度+歌词上下文
- lyrics — 完整歌词
- playlists — 歌单列表
- queue — 队列状态

## ✅ 2025-06-20 修复 (v1.3.0)
- **状态存储架构重构** — 去掉不可靠的 /tmp/ 文件存储，改为纯内存 + 播放器每3秒全量状态同步。解决 Vercel 多实例间状态不一致问题（queue 有歌但 current_song 返回空）。
- **add_to_playlist** — trackIds 格式改为 JSON 数组 `[songId]`
- **playlist_tracks** — n→200 + offset 分页 + trackIds 批量详情查询
- **getSongUrl** — 4 级回退（320k→128k→v1→outer URL）
- **debug 端点** — 增加全套 API 测试输出

## ❌ 还没修好的
- 手机浏览器偶尔缓存旧页面（加了 no-cache header）

## 🔧 乱切歌问题修复原理（重要！）

### 之前的问题
- 播放器每2秒轮询服务器 `/api/state`
- 服务器 `render()` 一收到新数据就切歌
- Vercel 多实例间状态不一致，服务器返回的 current song 会跳回旧歌
- 队列闪烁、切歌后歌消失、播一半跳到别的歌

### 修复方案：播放器管队列，服务器只提供新歌

**1. 播放器 `localQueue`（player/index.js 前端 JS）**
```
localQueue 只在以下情况变化：
  - 服务器有新歌 → push 到末尾（不去重）
  - 用户点 ⏭ → localQueue index+1  → playSong()
  - 用户点 ⏮ → localQueue index-1 → playSong()
  
  NEVER 被服务器覆盖！
```

**2. `render()` 只在不活跃时自动切歌**
```javascript
// 只在 !currentId（还没播任何歌）时才自动切
if (!currentId && d.current && d.current.id) {
    currentId = d.current.id;
    // ... 设置封面、歌名、播放
}
// 已经在播 → 啥也不做，不管服务器怎么说
```

**3. `playSong(t)` 统一切歌入口**
```javascript
function playSong(t) {
    currentId = t.id;
    // 更新封面、歌名、歌手
    // 调 resolveUrlFor(t.id) 拿音频
    // 不再碰服务器
}
```

**4. `next()` 完全本地**
```javascript
function next() {
    var idx = localQueue.findIndex(t => t.id === currentId);
    if (idx >= 0 && idx + 1 < localQueue.length) playSong(localQueue[idx+1]);
}
```

**5. 服务端 `mcpTouch()` 6 秒保护**
```
MCP 刚设的歌 → 6 秒内不被播放器上报覆盖
避免 MCP 设歌 → 播放器回传空数据 → 歌丢失的反馈回路
```

### 经验教训
- **Serverless 不能用服务器内存管队列** — 实例不共享
- **播放器自己管队列** — 单页应用天然稳定
- **服务器只管搜歌 + 解析 URL** — 无状态

## ⚠️ 硬伤：VIP歌曲
- 网易云 weapi 端点封了国外IP
- Vercel 服务器在美国 → weapi 不可用
- 所以 VIP/原版歌曲很多听不了（林俊杰、周杰伦等全是❌）
- 能听的是翻唱、小众歌手、部分英文歌（Justin Bieber 有一些）

## 🔑 解决方案
迁移到亚洲服务器（新加坡/香港），让 weapi 能通。
- Koyeb: 新加坡，免费 5$/月，但需要绑卡
- Fly.io: 香港，免费额度，需要绑卡（支持银联）
- 用户没有 Visa，有银联信用卡
- 已写了 server.js 准备迁移，等用户决定

## 潜在的坑（下个对话里注意）
1. 手机缓存很顽固，可能需要无痕模式测试
2. CLI 连不上 Vercel（TLS握手失败），只能用 GitHub push
3. 播放器队列已改为播放器自己管（localQueue），服务器不控制播放顺序
4. Cookie 存在 Vercel 环境变量里，看不到但能用
5. api/index.js 用旧版 API（/api/ 端点），server.js 用 weapi
