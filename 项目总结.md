# netease-music-mcp 项目总结

## 这是什么
一个 MCP Server，让你用 Claude（网页版+手机App）控制网易云音乐播放。搜歌、切歌、看歌词都能用。

## 怎么用
1. 手机浏览器打开 `https://netease-music-mcp.vercel.app/`（播放器页面）
2. Claude 网页版 Settings → MCP Servers → 添加：
   - Name: `netease-music`
   - URL: `https://netease-music-mcp.vercel.app/api/mcp`
3. 对 Claude 说 "放一首xxx"

## 项目文件
- `api/index.js` — Vercel 服务端函数（MCP + 播放器 HTML + 网易云接口）
- `api/index.js.backup` — 早期备份（v1.3.x 样式）
- `api/index.js.backup2` — 后期备份（v1.5.x 毛玻璃样式 + localStorage）
- `lib/netease.js` — 网易云 weapi 加密
- `lib/bg.js` — 背景图 base64（6.jpg）
- `server.js` — Koyeb 版本（weapi全功能，未部署）
- `vercel.json` — Vercel 配置（香港 hkg1）
- `D:\Claude Code\cookie.txt` — 完整 Cookie（2600字符）

## GitHub
- Repo: `https://github.com/SHY0609/netease-music-mcp`
- 分支: `main`（也推到 `master`）
- Vercel 连着 GitHub，推送自动部署

## Vercel
- 项目: hailey9/netease-music-mcp
- 区域: **香港 hkg1**（vercel.json functions.regions）
- URL: `https://netease-music-mcp.vercel.app`

## ✅ 当前版本功能 (v1.5.x)

### MCP 工具
- play — 搜索并播放（4 级 URL 回退：320k→128k→v1→outer）
- skip — 切下一首
- pause — 暂停/恢复
- current_song — 当前歌曲+播放进度+歌词
- lyrics — 完整歌词
- playlists — 获取歌单列表
- playlist_tracks — 获取歌单内歌曲（分页支持）
- add_to_playlist — 添加到歌单（trackIds JSON 数组格式 `[songId]`）
- queue — 队列状态

### 播放器前端
- **毛玻璃 UI** — `backdrop-filter: blur`，半透明卡片
- **背景图** — 6.jpg 全屏 + 暗色渐变遮罩
- **专辑封面** — `extractCover()` 4 种方式构造（picUrl → picId+picStr → picId → 空）
- **localStorage 持久化** — 刷新不丢歌
- **清空按钮** — 队列标题旁，5 秒防回弹
- **local mcpSwitch** — 浏览器本地 `nm_lastMcp` 时间戳判断 MCP 切歌指令
- **版本化 URL** — `/?v=1.5.1` 强制绕过手机缓存

### 服务端架构
- **纯内存状态** — `_state` 全局对象，`getPlayer()` 同步
- **mcpTouch()** — MCP 写入后 6 秒内不被播放器同步覆盖
- **mergeFromPlayer()** — 播放器每 3 秒上报全量状态
- **/api/clear** — 清空服务器状态
- **/api/debug** — 完整 API 测试（含 `?addPid=X&addSid=Y`）
- **/api/ping** — 部署版本验证

## ⚠️ 已知限制
- **Vercel 多实例** — 香港单区域通常只有 1 实例，极端情况下可能不一致
- **手机缓存** — Safari 无视 Cache-Control，建议无痕模式
- **VIP 歌曲** — Vercel 旧 API（非 weapi）部分 VIP 歌可能无播放链接
- **weapi 不可用** — 需迁移到亚洲服务器（server.js 已写好，待部署）

## 🔧 关键修复历史

### v1.5.x — UI 重做 + localStorage
- 毛玻璃播放器、6.jpg 背景、琥珀色主题
- localStorage 持久化队列
- 清空按钮 + 5 秒防回弹
- local mcpSwitch 自动切歌
- extractCover 封面多源提取
- 香港 hkg1 区域固定

### v1.3.x — 状态重构
- /tmp/ → 纯内存状态
- add_to_playlist trackIds JSON 数组格式
- playlist_tracks 分页 + 批量详情
- getSongUrl 4 级回退
- mcpTouch 6 秒保护

### v1.2.0 — 基础功能
- 全部 MCP 工具可用

## ⚡ 部署注意事项
1. **推送前必须** `node --check api/index.js` 验证语法
2. **JS 花括号必须平衡** — 前端 JS 在 HTML 模板内，多了 `}` 整个播放器不渲染
3. 推 `main` 分支（Vercel 从 `master` 拉）
4. Vercel 构建日志会显示 "Restored build cache" — 正常，只要语法对就能部署
5. CLI `npx vercel --prod --yes` 连不上（TLS），用 git push

## 🔑 未来方向
迁移到 Koyeb/Fly.io 亚洲服务器，解锁 weapi → VIP 歌曲全通。
