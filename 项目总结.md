# netease-music-mcp 项目总结

## 这是什么
一个全功能 MCP Server，让 Claude（桌面版+手机App）控制 **网易云音乐播放 + 美团外卖点餐 + 淘宝搜索 + 长期记忆**。搜歌、切歌、看歌词、搜外卖、看菜单、下单、搜商品、跨对话记忆都能用。

## 怎么用
1. 打开 `https://yuuke.online/`（播放器页面）
2. Claude Settings → MCP Servers → 添加：
   - Name: `netease-music`
   - URL: `https://yuuke.online/api/mcp`
3. 对 Claude 说 "放一首xxx" 或 "搜一下附近的奶茶"

## 部署

| 项目 | 详情 |
|------|------|
| 服务器 | 腾讯云轻量应用服务器 2核4G · 40G SSD |
| 系统 | Ubuntu 22.04 |
| 域名 | **https://yuuke.online/** |
| SSL | Let's Encrypt（自动续期，2026-09-30 到期） |
| 进程管理 | systemd（`netease-music-mcp.service`） |
| 记忆库 | Docker（`ombre-brain` 容器，`127.0.0.1:18001`） |
| Vercel | `https://netease-music-mcp.vercel.app`（备用，已不维护） |

## 项目文件
- `server.js` — **主文件**（腾讯云运行），全功能 HTTP 服务器：MCP + weapi + 美团 + 淘宝 + 播放器
- `api/index.js` — Vercel 版（旧版，仅备用），serverless handler
- `lib/netease.js` — 网易云 weapi 加密（国内 IP 全通）
- `lib/mt-signer-v2.cjs` — 美团 mtgsig v1.2 签名生成器（jsdom + h5guard）
- `lib/runtime.js` — webpack chunk loader（浏览器抓取，15KB）
- `lib/h5guard-v2.js` — H5guard v1.2 引擎（CDN 下载，346KB）
- `lib/taobao.js` — 淘宝搜索/详情 API
- `lib/bg.js` — 播放器背景图 base64
- `vercel.json` — Vercel 配置（香港 hkg1）
- `C:\Users\lenovo\.claude\memory\` — Cookie 文件存放处

## GitHub
- Repo: `https://github.com/SHY0609/netease-music-mcp`
- 分支: `main`（也推到 `master`）

## ✅ 当前版本功能 (v3.1)

### 🧠 长期记忆 MCP 工具（12个，Ombre-Brain 转发）
- breath — 睁眼浮现记忆（无参=权重浮现 / 带 query=关键词+语义检索）
- hold — 记下一件事（自动情感打标，与近似桶自动合并）
- grow — 整理长内容（自动拆成 2~6 条独立桶）
- trace — 修正记忆（resolved/pinned/content/删除等）
- dream — 做梦消化最近变动的记忆
- anchor / release — 设/解坐标系（硬上限 24）
- pulse — 自检记忆系统状态
- plan — 登记承诺/待办（不衰减、自动闭环检测）
- letter_write / letter_read — 写信/读信（原文永久保留）
- I — 自我认知（记录"我是什么"，不参与普通浮现）
- **原理**: server.js 启动时连接本地 Ombre-Brain（Docker），记忆工具调用透明转发，会话过期自动重连
- **LLM（压缩/打标）**: DeepSeek `deepseek-chat`（国内直连，充 10 块够用半年）
- **嵌入（语义检索）**: SiliconFlow `BAAI/bge-m3`（免费额度）
- **存储**: Markdown 文件 + YAML frontmatter + SQLite 向量索引，Obsidian 原生兼容
- **遗忘曲线**: 改进版艾宾浩斯公式，高情绪强度记忆衰减更慢，pinned 永不衰减

### 🎵 网易云音乐 MCP 工具（9个）
- play — 搜索并播放（weapi 加密，VIP 全通）
- skip — 切下一首
- pause — 暂停/恢复
- current_song — 当前歌曲+播放进度+歌词上下文
- lyrics — 完整歌词
- playlists — 获取歌单列表
- playlist_tracks — 获取歌单内歌曲（分页支持）
- add_to_playlist — 添加到歌单
- queue — 队列状态

### 🍔 美团外卖 MCP 工具（4个）
- mt_search — 搜索店铺（40+ 参数完整构造，_token deflate+base64）
- mt_menu — 获取店铺完整菜单（两颗星流程：poi/food → tags → menuproducts）
- mt_addresses — 获取收货地址（code:0）
- mt_order — **预览+下单**（自动地址匹配、坐标填充、优惠券、打包费、confirm=true 真下单）

### 🛒 淘宝 MCP 工具（2个）
- tb_search — 搜索商品（关键词、价格区间、排序、分页）
- tb_detail — 商品详情（价格、优惠券、SKU、店铺信息）

### 🎵 播放器前端
- 毛玻璃 UI（`backdrop-filter: blur`）+ 全屏背景 + 琥珀色主题
- 专辑封面自动构造（4种方式回退）
- localStorage 持久化队列（刷新不丢歌）
- MCP 切歌指令自动同步（`nm_lastMcp` 时间戳）
- 版本化 URL（`/?v=1.5.1`）绕过手机缓存

### 🔐 mtgsig v1.2 签名引擎
- **架构**: jsdom 模拟浏览器 → runtime.js (webpack) → H5guard.js (v1.2引擎)
- **签名速度**: ~900ms 首次，后续 ~15ms
- **原理**: runtime.js 创建 webpack 模块系统 → H5guard.js 注册 → window.H5guard.sign() 生成 v1.2 格式签名
- **验证**: 搜索/地址/菜单/下单预览 均 code:0

### 🍔 美团 API 接口清单

| 接口 | 端点 | 状态 | 说明 |
|------|------|------|------|
| 搜索 | `/openh5/search/globalpage` | ✅ code:0 | 返回20家店+菜品+SKU+价格 |
| 地址 | `/openh5/address/list` | ✅ code:0 | 返回用户保存的收货地址 |
| 初始化 | `/openapi/v1/poi/food` | ✅ code:0 | 返回所有 tag 列表 |
| 菜单 | `/openh5/v2/poi/menuproducts` | ✅ | tag → 完整菜单 |
| 下单预览 | `/openh5/order/v2/preview` | ✅ code:0 | 价格计算通过 |
| 下单提交 | `/openh5/order/v2/submit` | ✅ code:0 | confirm=true 到达美团服务器 |

### 🔑 关键突破

1. **mtgsig v1.2 签名引擎**: 9小时攻克，jsdom + runtime + H5guard
2. **weapi 全通**: 腾讯云国内 IP → VIP歌曲随便播
3. **万能菜单方案**: poi/food 自动获取 tag → menuproducts 拿完整菜单
4. **下单全链路**: 搜索 → 地址 → 菜单 → 预览 → 提交（confirm=true）
5. **自动地址匹配**: addressId → 完整姓名/电话/地址/坐标

## ⚠️ 已知限制
- **美团 418 风控**: 高频测试会触发 `riskLevel:199` 人机验证，需要在浏览器 `h5.waimai.meituan.com` 完成滑块解锁
- **Cookie 有效期**: 美团/网易云 Cookie 需定期从浏览器导出更新
- **多商品下单**: 当前仅支持单商品，需扩展 foodlist
- **菜单打烊**: 深夜部分店铺 poi/food 返回空 tag
- **海外 API 被墙**: Google Gemini、Vercel、Cloudflare Workers 均无法从服务器直连，记忆库 LLM 必须用国内服务
- **记忆导入**: Dashboard 文档导入功能不稳定（LLM 格式解析问题），推荐通过 Claude 对话逐步写入记忆
- **GitHub Push**: 国内网络不稳定，代码更新需 SCP 传文件到服务器

## 🔧 修复历史

### v3.1 — Ombre-Brain 记忆库集成（2026-07-03 ~ 07-04）
- Docker 部署 Ombre-Brain（`127.0.0.1:18001`），12 个记忆工具完整可用
- server.js 启动时自动 MCP 握手 + 动态获取记忆工具列表
- 记忆工具调用透明转发（SSE 响应解析适配、会话过期自动重连）
- Ombre-Brain 不可用时优雅降级（不注册记忆工具，音乐/美团不受影响）
- **LLM 方案切换**：Gemini（被墙）→ Cloudflare Workers 代理（workers.dev 被墙）→ Vercel 代理（被墙）→ 最终 DeepSeek + SiliconFlow（国内直连）
- **踩坑**: Ombre-Brain API Key 需同时配在 `.env` 和 `config.yaml` 的 `dehydration.api_key` 字段，仅设环境变量不够
- 端口仅监听本地（安全），OAuth 关闭（内网无需鉴权）
- Dashboard 通过 SSH 隧道访问：`ssh -L 18001:127.0.0.1:18001 ubuntu@139.199.89.55`

### v3.0 — 腾讯云部署 + HTTPS + MCP SSE 修复（2026-07-03）
- 腾讯云轻量服务器部署：Ubuntu 22.04 + Node 24 + Nginx + systemd
- 域名 yuuke.online + Let's Encrypt 免费 SSL（自动续期）
- 修复 MCP POST handler 重复 writeHead 导致服务崩溃
- weapi 全通（国内 IP），VIP 歌曲随便播
- 美团 418 自动重试机制（mtApi 统一处理）
- 浏览器伪装头增强：sec-fetch / x-requested-with

### v2.3 — 下单功能完善（2026-07-02）
- mtPlaceOrder / mtOrderSubmit 自动获取完整地址信息
- mt_order 不传 addressId 时自动选第一个地址兜底
- 预览增强：打包费、优惠券、orderToken、收货地址
- 提交保护：缺地址时拒绝提交，返回明确指引

### v2.x — 美团集成 + mtgsig 逆向（2026-06-22）
- mtgsig v1.2 签名引擎（9小时攻克）
- 美团搜索/地址/菜单/下单全套 API
- 万能菜单方案：poi/food → tags → menuproducts
- debug 端点：`/api/debug?key=debug&mt=关键词`

### v1.5.x — UI 重做（毛玻璃 + localStorage）
### v1.3.x — 状态重构（/tmp/ → 内存）
### v1.2.0 — 基础 MCP 工具

## 🔧 服务器维护
```bash
# SSH 连接
ssh ubuntu@139.199.89.55

# ── netease-music-mcp ──
# 查看日志
sudo journalctl -u netease-music-mcp -f

# 更新代码（GitHub 被墙，需 SCP 上传或配代理）
cd /home/ubuntu/netease-music-mcp
# git pull  # 大概率超时
scp server.js ubuntu@139.199.89.55:/home/ubuntu/netease-music-mcp/
sudo systemctl restart netease-music-mcp

# 更新 Cookie
sudo nano /etc/netease-music-mcp/env
sudo systemctl restart netease-music-mcp

# ── Ombre-Brain 记忆库 ──
# 查看状态
docker ps | grep ombre
curl http://localhost:18001/health

# 重启
cd ~/ombre-brain
docker compose -f docker-compose.user.yml restart

# 重建（更新镜像）
docker compose -f docker-compose.user.yml down
docker compose -f docker-compose.user.yml pull
docker compose -f docker-compose.user.yml up -d

# 查看记忆数据（Obsidian 兼容的 md 文件）
ls ~/ombre-brain/buckets/dynamic/
cat ~/ombre-brain/buckets/dynamic/*.md  # 直接看原始内容

# 更新 API Key（.env + config.yaml 都要改！）
nano ~/ombre-brain/.env
sudo nano ~/ombre-brain/buckets/config.yaml   # dehydration.api_key 也要填
docker compose -f docker-compose.user.yml down && docker compose -f docker-compose.user.yml up -d
```

### Ombre-Brain .env 模板
```bash
# LLM 压缩/打标 — DeepSeek
OMBRE_COMPRESS_API_KEY=sk-你的Key
OMBRE_COMPRESS_BASE_URL=https://api.deepseek.com/v1/
OMBRE_COMPRESS_MODEL=deepseek-chat
# 向量嵌入 — SiliconFlow（免费）
OMBRE_EMBED_API_KEY=sk-你的Key
OMBRE_EMBED_BASE_URL=https://api.siliconflow.cn/v1/
OMBRE_EMBED_MODEL=BAAI/bge-m3
# 存储
OMBRE_HOST_VAULT_DIR=/home/ubuntu/ombre-brain/buckets
OMBRE_TRANSPORT=streamable-http
OMBRE_MCP_REQUIRE_AUTH=false
OMBRE_HOST_PORT=18001
```

## 🔑 未来方向
1. **下单支付验证**: 小金额真实下单，确认 payUrl 是否返回支付链接
2. **多商品下单**: 支持 foodlist 多个商品
3. **优惠券集成**: 提交时自动选最优优惠券
4. **Cookie 自动刷新**: 定时从浏览器同步 Cookie
5. **搜索结果排序**: 按距离/价格优化
